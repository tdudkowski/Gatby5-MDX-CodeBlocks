---
title: "First Blognote"
excerpt: "Text is here."
---

## First subtitle

Text is here. Text is here.

```js file=fizzbuzz.js highlights=6 numberLines
// fizzbuzz in JS
for (let i = 1; i <= 100; i++) {
  let out = "";
  if (i % 3 === 0) out += "Fizz";
  if (i % 5 === 0) out += "Buzz";
  console.log(out || i);
}
```

## Second subtitle

Text is here. Text is here.

```jsx filename=fancy-button.js
// here's a button in React!
<button
  onClick={() => {
    alert("Hello MDX!");
  }}
>
  test
</button>
```

Text is here. Text is here.